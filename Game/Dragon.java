public class Dragon {
    /**
     * Used to create a dragon.
     * Should store the dragon health and level (1, 2, or 3).
     * Should have methods to attack (including determine attack amount), handle an incoming attack (including updating the dragon’s own health), and determine which of the four outcomes happen when it dies.
     * Should include any other static or instance variables and/or methods, including getters/setters and private helper methods, that you determine are necessary to implement the requirements.
     */
}
